{
  "name": "kudo",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build chapter02/src/*.ts --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^3.1.1"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
